Python is a case sensitive language - "A" is not seen as "a"

Variables are a way of storing value. Put simply it is a name that represent a value.
Rules for Naming Variable in Python
1. variables shoud not have spaces - dat of bith (invalid, date_of_bith (valid)
2. variable do not start with numbers - 12strong, 1hundred (invalid); strong12, hundred1,ten10two (valid)
3. variables should not ne the same with keywords or reserved words (identifiers)=